#include "main.h"

/**
 * _strlen - determines the length of a string
 *
 * @s: a given string
 *
 * Return: the length of the string
 */
int _strlen(char *s)
{
	int i, length;

	for (i = 0; s[i] != '\0'; i++)
	{
		length = i;
	}
	return (length);
}
